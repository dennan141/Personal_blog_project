<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Update {{title}}</title>
    <link rel="stylesheet" type="text/css" href="../main.css">
</head>


{{#if validationErrors.length}}
<div class="container card text-center card w-100 mt-5" style="width: 20rem;">
    <div>Can't create blogpost because:</div>
    <ul>
        {{#each validationErrors}}
        <li>{{this}}</li>
        {{/each}}
    </ul>
</div>
{{/if}}

<body>
    <main class="containter mt-5 text-center mb-5">
        <form action="" method="POST" action="/portfolio/update-project/"{{id}}>
            <h1>Update "{{project.title}}"</h1>
            <strong>Title</strong>
            <div>
                <input type="text" name="title" value="{{project.title}}">
            </div>
            <div class="mt-3"><strong>Content</strong> </div>
            <div>
                <textarea name="content" cols="100" rows="15">{{project.content}}</textarea>
            </div>
            <div class="mt-3"><strong>Description</strong></div>
            <div>
                <textarea name="description" cols="100" rows="5">{{project.description}}</textarea>
            </div>
            <input class="mt-2" type="submit" value="Update Proejct">
        </form>
    </main>
</body>

</html>