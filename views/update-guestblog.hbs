<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Update {{guestblogpost.title}}</title>
    <link rel="stylesheet" type="text/css" href="../main.css">
</head>

{{#if validationErrors.length}}
<div class="container card text-center card w-100 mt-5" style="width: 20rem;">
    <div>Can't create blogpost because:</div>
    <ul>
        {{#each validationErrors}}
        <li>{{this}}</li>
        {{/each}}
    </ul>
</div>
{{/if}}


<body>
    <main class="containter mt-5 text-center">
        <form action="" method="POST" action="/update-blogpost/"{{id}}>
            <h1>Update "{{guestblog.guest_title}}"</h1>
            <strong>Title</strong>
            <div>
                <input type="text" name="title" value="{{guestblog.guest_title}}">
            </div>
            <strong>Name</strong>
            <div>
                <input type="text" name="name" value="{{guestblog.guest_name}}">
            </div>
            <div class="mt-3"><strong>Content</strong> </div>
            <div>
                <textarea name="content" cols="100" rows="15">{{guestblog.guest_content}}</textarea>
            </div>
            <div class="mt-3"><strong>Description</strong></div>
            <div>
                <textarea name="description" cols="100" rows="5">{{guestblog.guest_description}}</textarea>
            </div>
            <input class="mt-2 mb-5" type="submit" value="Update Blogpost">
        </form>
    </main>
</body>
</html>