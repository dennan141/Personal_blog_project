<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Update {{guestblogpost.title}}</title>
    <link rel="stylesheet" type="text/css" href="../main.css">
</head>

{{#if errors.length}}
<div class="container card text-center card w-100 mt-5" style="width: 20rem;">
    <div>Can't create blogpost because:</div>
    <ul>
        {{#each errors}}
        <li>{{this}}</li>
        {{/each}}
    </ul>
</div>
{{/if}}


<body>
    <main class="containter mt-5 text-center">
        <form action="" method="POST" action="/update-blogpost/"{{id}}>
            <h1>Update "{{guestblogposts.guest_title}}"</h1>
            <strong>Title</strong>
            <div>
                <input type="text" name="title" value="{{guestblogposts.guest_title}}">
            </div>
            <strong>Name</strong>
            <div>
                <input type="text" name="name" value="{{guestblogposts.guest_name}}">
            </div>
            <div class="mt-3"><strong>Content</strong> </div>
            <div>
                <textarea name="content" cols="100" rows="15">{{guestblogposts.guest_content}}</textarea>
            </div>
            <div class="mt-3"><strong>Description</strong></div>
            <div>
                <textarea name="description" cols="100" rows="5">{{guestblogposts.guest_description}}</textarea>
            </div>
            <input class="mt-2 mb-5 btn btn-primary" type="submit" value="Update Blogpost">
        </form>
    </main>
</body>
</html>