<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{guestblogposts.title}}</title>
</head>

<body>

        <main class="container text-center mt-5 mb-5" >
        <div class="container mt-5">
            <div class="card text-center card w-100" style="width: 20rem;">
                <h1 class="text-center ml-5 mr-5">{{guestblogposts.guest_title}}</h1>
                <p class="text-center ml-5 mr-5">{{guestblogposts.guest_content}}</p>
                <div class="mt-3 ml-5 mr-5">Author: {{guestblogposts.guest_name}}</div>
            </div>
        </div>
    </main>

    <footer class="container mb-5 mt-5">
        <div class="text-center">Will be hidden later</div>
        <div>
            <form action="/guestblog/delete-guestblog/{{guestblogposts.id}}" method="POST">
                <button type="submit" class="btn btn-danger">Delete</button>
            </form>
        </div>
        <div>
            <form action="/guestblog/update-guestblog/{{guestblogposts.id}}" method="GET">
                <button type="submit" class="btn btn-info">Update</button>
            </form>
        </div>
    </footer>
</body>

</html>