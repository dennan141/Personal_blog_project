<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Guest-Blog</title>
</head>

<body>
    <h1 class="text-center display-2">Guest Blogs</h1>
    <div class="text-center">
        <a href="/guestblog/create-blogpost" class="btn btn-primary btn-center" style="width: 15rem;" >Create new Blogpost</a>
    </div>
    

    <main>
        <div class="container">
            <div class="col">
                {{#if guestblogposts.length}}
                {{#each guestblogposts}}
                <div class="col sm-6 my-5">
                    <div class="card text-center card w-100" style="width: 20rem;">
                        <div class="card-body">
                            <h5 class="card-title">{{guest_title}}</h5>
                            <p class="card-text">
                                {{guest_description}}
                            </p>
                            <a href="/guestblog/{{id}}" class="btn btn-primary">Read More</a>
                        </div>
                    </div>
                </div>
                {{/each}}
                {{else}}
                <p>There are currently no guestblogposts avalible. Feel free to make one!</p>
                {{/if}}
            </div>
        </div>
    </main>

</body>

</html>